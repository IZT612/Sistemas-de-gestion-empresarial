@model List<UI.Models.IndexViewModel>

<h2>Misiones</h2>

<select id="misionesDropdown">
    @foreach (var mision in Model)
    {
        <option value="@mision.Id">@mision.Titulo</option>
    }
</select>

<button id="verDetallesBtn">Ver detalles</button>

<div id="detallesMision">
    <!-- Aquí mostrarás los detalles con JavaScript o un postback -->
</div>
