import { Routes } from '@angular/router';
import { ListaPersonasComponent } from '../UI/Components/lista-persona/lista-personas';

export const routes: Routes = [
  {
    path: '',
    component: ListaPersonasComponent,
  }
];
